# Test Directory

All project tests live in this folder.

Suggested layout:

- `test/auth/` for authentication and session coverage
- `test/unit/` for shared unit tests
- `test/integration/` for route, API, and server integration tests
- `test/setup.ts` for shared Vitest setup
